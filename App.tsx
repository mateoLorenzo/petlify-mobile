import React from 'react';
import {
  SafeAreaView,
  StatusBar,
  StyleSheet,
  Text,
  TextInput,
  TouchableOpacity,
  View,
} from 'react-native';
import LoginScreen from './src/screens/Login';

const App = () => {
  return <LoginScreen />;
};

export default App;
